language: python
matrix:
  include:
    - os: linux
      python: 3.6
before_install:
  - pip install --upgrade pip
  - pytest --version
install:
  - sh prepare.sh
  - pip install -r requirements.txt
  - pytest --version
script:
  - cd tests
  - pytest --version
